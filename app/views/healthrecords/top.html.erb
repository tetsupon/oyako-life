<div class="health-records-container">
  <h2><%= @child.name %>の健康記録管理</h2>

  <div class="health-record-links">
    <%= link_to '予防接種記録', child_vaccinations_path(@child), class: 'btn btn-primary' %>
    <%= link_to '健康診断記録', child_healthchecks_path(@child), class: 'btn btn-primary' %>
  </div>
</div>
